import { Register } from "@/features/auth/register/pages";

export const metadata = {
  title: "Register Page",
  description: "Register Page",
};

const RegisterPage = () => {
  return <Register />;
};

export default RegisterPage;
